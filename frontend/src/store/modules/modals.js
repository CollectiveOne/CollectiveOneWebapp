const state = {
  showNewInitiative: false
}

const getters = {
}

const mutations = {
  showNewInitiativeModal: (state, payload) => {
    state.showNewInitiative = payload
  }
}

const actions = {
}

export default {
  state,
  getters,
  mutations,
  actions
}
