import Vue from 'vue'
import ModelSectionModal from '@/components/model/modals/ModelSectionModal.vue'

describe('ModelSectionModal', () => {
  it('add a detached copy of an existing section', () => {
    console.dir('here')
  })
})
